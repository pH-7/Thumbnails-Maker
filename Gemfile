source "https://rubygems.org"

# Specify your Ruby version if needed (comment this out if running into issues)
# ruby "3.0.0"

# Primary dependencies for app distribution
gem "fastlane", "~> 2.200.0"
gem "cocoapods", "~> 1.11.3"

# Additional Fastlane plugins
gem "fastlane-plugin-appicon"  # For generating app icons
gem "fastlane-plugin-versioning" # For managing version numbers
gem "dotenv" # For loading environment variables

# Install all needed dependencies using:
# bundle install --path vendor/bundle
#
# Then run fastlane with:
# bundle exec fastlane [lane_name]
#
# For more information about the Gemfile, see:
# https://bundler.io/gemfile.html
